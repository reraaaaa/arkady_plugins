# Q&A processor

## Overview

The QA processor is a service that provides question&answer chunking mode, the chunks retrieved and recalled are the same.

## Installation

Install from the plugin's GitHub release (`.arkadypkg` asset) in Arkady's plugin console, or reference it directly from a pipeline template via a `github` dependency.
