[project]
name = "arkady-parentchild-chunker"
version = "0.0.15"
description = "Parent-child chunking tool"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "arkady-plugin-sdk>=0.10.4",
    "pydantic>=2.13.4",
    "tiktoken>=0.13.0",
    "transformers>=5.9.0",
]

# uv run black . -C -l 100 && uv run ruff check --fix
[dependency-groups]
dev = []
