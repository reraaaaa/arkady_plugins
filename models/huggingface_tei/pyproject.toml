[project]
name = "huggingface_tei"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"

# Managed with uv; refresh the lockfile with `uv lock`.
dependencies = [
    "arkady-plugin-sdk>=0.10.4",
    "httpx>=0.28.1",
    "requests>=2.34.2",
    "yarl>=1.24.2",
]

# uv run black . -C -l 100 && uv run ruff check --fix
[dependency-groups]
dev = []
