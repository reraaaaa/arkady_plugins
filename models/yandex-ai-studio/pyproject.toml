[project]
name = "arkady-yandex-ai-studio"
version = "0.1.0"
description = "Chat models via Yandex AI Studio (OpenAI-compatible REST, Api-Key auth)."
readme = "README.md"
requires-python = ">=3.12"

# Refresh the lockfile with: uv lock
dependencies = [
    "arkady_plugin>=0.10.0",
    "requests>=2.34.2",
    "tiktoken>=0.13.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "ruff>=0.15.14",
]
